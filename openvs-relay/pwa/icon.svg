<!-- OpenVS Relay — Cloudflare Worker + Durable Object + PWA
     Placeholder app icon: a single-color rounded square with the initial "V". Real icon assets
     (a proper mark, PNG/maskable variants for platforms that don't accept SVG manifest icons —
     notably iOS's apple-touch-icon) are a later polish item, not blocking for this phase. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 192 192">
	<rect width="192" height="192" rx="36" fill="#111318"/>
	<text x="96" y="128" font-family="system-ui, sans-serif" font-size="112" font-weight="700" fill="#7dd3fc" text-anchor="middle">V</text>
</svg>
